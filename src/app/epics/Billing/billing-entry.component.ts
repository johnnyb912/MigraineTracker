import {Component, ChangeDetectionStrategy} from '@angular/core';

@Component({
    templateUrl     : './billing-entry.component.html',
    styleUrls       : [ './billing-entry.component.scss' ],
    changeDetection : ChangeDetectionStrategy.OnPush
})

/**
 * implementation for BillingEntryComponent: responsible for billing epic layout and routing
 */
export class BillingEntryComponent {
    /**
     * BillingEntryComponent constructor
     */
    constructor() {}
}
